name: Construcció Android i comprovacions
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
concurrency:
  group: comprarador-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Normalitzar els catàlegs originals i executar les proves
        shell: bash
        run: |
          set -euo pipefail
          python tools/build_catalog.py --download --allow-rejects
          python -m unittest discover -s tests -v
          python - <<'PY'
          import json
          from pathlib import Path
          informe = json.loads(Path('app/src/main/assets/catalog.report.json').read_text())
          assert informe['integrity'] == 'ok'
          assert informe['products_total'] > 15000
          assert informe['indexed_total'] == informe['products_total']
          print('Referències històriques:', informe['products_total'])
          PY
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Preparar l'SDK d'Android
        shell: bash
        run: |
          set -euo pipefail
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          test -x "$SDKMANAGER"
          (yes | "$SDKMANAGER" --licenses > /dev/null) || true
          "$SDKMANAGER" --install 'platforms;android-35' 'build-tools;35.0.0'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Compilar l'APK de depuració
        run: gradle :app:assembleDebug --no-daemon --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: Comprarador-v0.1.1-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
      - name: Etiquetar i publicar la revisió 0.1.1
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if ! git ls-remote --exit-code --tags origin refs/tags/v0.1.1 > /dev/null 2>&1; then
            git -c user.name='Manel' -c user.email='Arcadialliure@outlook.com' tag -a v0.1.1 -m 'Comprarador 0.1.1'
            git push origin refs/tags/v0.1.1
          fi
          if ! gh release view v0.1.1 > /dev/null 2>&1; then
            cp app/build/outputs/apk/debug/app-debug.apk Comprarador-v0.1.1.apk
            gh release create v0.1.1 Comprarador-v0.1.1.apk \
              --title 'Comprarador 0.1.1 · prova Android' \
              --notes 'Versió de prova: escaneig directe, targetes OCR, comparació amb la darrera compra i gràfic del 2003 i les compres desades. APK de depuració; no és una versió comercial signada. Les claus de depuració poden variar entre compilacions: no desinstal·leu una versió anterior sense preservar les dades locals.'
          fi
